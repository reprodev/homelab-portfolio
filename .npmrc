# @react-three/fiber declares optional Expo / react-native peers that npm tries
# to resolve and which conflict with React 18. This is a web-only Vite app, so we
# skip peer-dependency auto-resolution to keep `npm install` (local + CI) clean.
legacy-peer-deps=true
